export enum VSCodeActions {
    splitTerminals = 'workbench.action.terminal.split',
    renameTerminal = 'workbench.action.terminal.renameWithArg',
    openAutoTerminalSettings = 'vscode.open',
    addFolderToWorkspace = 'workbench.action.addRootFolder',
    formatFile = 'editor.action.formatDocument',
}
